# NURA SKILL POLICY & PROCEDURE MANUAL (2026-08-06, founder directive)

**The operating law: every skill carries a POLICY (when it fires, who authorizes, the boundaries) and a PROCEDURE (the exact steps, the verification gates). The activation audit (08-06): all skills loadable ✓ · the 9 previously-pruned skills reloaded and re-verified ✓ · the new skills (mechanistic-interpretation · agentchat-etiquette · paperclip-hermes-adapter-ops · url-integration-review · vault-shared-memory) registered ✓.**

## The P&P template (every skill follows this shape)
```
POLICY: the trigger · the authority (who can invoke/what's gated) · the boundaries (the never-list)
PROCEDURE: the numbered steps · the verification gate (the evidence before the claim)
```

---

## 1. emergency-medicine-reference (health — MANDATORY, non-removable)
- POLICY: fires on ANY acute/critical clinical content. Authority: licensed clinician review before consequential action — the agent NEVER signs off alone. Boundaries: ABCDE-first, no fake reviews, data-source safeguards.
- PROCEDURE: triage ABCDE → Emergency Safety Review template → the evidence retrieval (the clinical MCP lanes) → the clinician-ready summary → the escalation if any mandatory trigger (airway/shock/neuro/ACS/sepsis/anaphylaxis/trauma/tox/SI-HI).

## 2. mechanistic-interpretation (software-development — NEW 08-06)
- POLICY: fires on any new/updated model lane, unexplained behavior, or red-team finding needing the "why". Authority: audit = required before any sovereign/clinical model lane reaches production. Boundaries: PHI-free corpora, Lab-only compute, findings = data-not-authority, unexpected findings = founder escalation.
- PROCEDURE: target → behavior-corpus → logit-lens → SAE-cache → circuit-hunt (activation-patching) → safety-mapping → evidence-table report → vault register (MI-audits/) → nura-ai-evaluation-monitoring integration.

## 3. ensemble-reasoning (software-development)
- POLICY: fires when accuracy matters (clinical drafts, CTO decisions, complex verification). Authority: router + verifier + consensus gate; the deterministic truth layer; the budget gate. Boundaries: no 100%-claims, the clinician signs.
- PROCEDURE: route (DeepSeek fast / Gemini vision / Claude clinical / OpenAI structured / Grok second-opinion-when-keyed) → verify (cross-lane critique → revise) → consensus (HIGH = confidence / disagreement = review queue) → log every run.

## 4. url-integration-review (devops — the founder's 08-05 mandate)
- POLICY: fires weekly (Monday 09:00 EST cron) + on every new URL/repo/hire list. Authority: the founder's standing directive — every link assessed, nothing dropped. Boundaries: the supply-chain scan before integration, the PHI-never, the never-blind-installs.
- PROCEDURE: collect (session_search) → classify (official/community/recon/skill/hire) → fit-map (clinical/network/infra/org/AV/skills) → decide (INTEGRATE/STUDY/MONITOR/SKIP-with-reason) → register (URL-Integration-Registry.md) → report.

## 5. agentchat-etiquette (social-media — NEW 08-06)
- POLICY: fires when operating the AgentChat agent network. Authority: founder-gated registration + outreach; the handle = NURATECH's discretion. Boundaries: PHI-NEVER (non-clinical chatter only), no secrets, audit-visible, Mattermost/Telegram stay canonical.
- PROCEDURE: read the full manual (cache) → register (API key, founder-gate) → the SDK (agentchatme) → verify (test-DM to @chatfather → evidence) → operate per the etiquette doctrine.

## 6. paperclip-hermes-adapter-ops (devops — NEW 08-06)
- POLICY: fires when Hermes operates as a Paperclip employee. Authority: the adapter registration + the founder-gated server restart. Boundaries: issue-scoped work, PHI-out, audit-visible heartbeats.
- PROCEDURE: npm build → registry.ts wiring (hermes_local) → agent creation → testEnvironment → test-issue → heartbeat → transcript evidence.

## 7. vault-shared-memory (note-taking — NEW 08-06)
- POLICY: fires when wiring the vault as cross-agent memory (Claude Code/Codex/Gemini-local/Grok/Ollama/Hermes). Authority: the founder's doctrine. Boundaries: the shared meta = the non-PHI lane, no secrets, one-file-at-a-time writes.
- PROCEDURE: meta/ (5 files) → the pointer-files (CLAUDE.md/AGENTS.md/GROK.md/gemini-local-prompt.md) → the cross-tool test (A-writes → B-reads → C-summarizes).

## 8. nura-env-setup (devops)
- POLICY: fires on Python deps / venv / test-lane issues. Authority: autonomous. Boundaries: never touch the root-owned venv directly — the uv-target lane.
- PROCEDURE: uv pip install --python /usr/bin/python3 --target (the ABI pin!) → PYTHONPATH verify → the compile-sweep → the test-lane (pytest + the acp extra + the mcp-SDK reinstall) → the evidence.

## 9. hermes-dojo (devops — the overnight gym)
- POLICY: fires on /dojo or the overnight cron. Authority: autonomous skill-improvement. Boundaries: never modify bundled skills, only the user skills; accept-evolution-only-if-score-improves.
- PROCEDURE: analyze (monitor.py) → improve (patch/create/evolve) → verify (re-measure, <5% = flag) → report → track (metrics history).

## 10. daily-self-reflection (devops — the silent loop)
- POLICY: fires daily (the cron, silent). Authority: autonomous fixing. Boundaries: escalate only real incidents/approval-tier changes; silent unless something needs the founder.
- PROCEDURE: thought-process → configs → DBs/memory → skills → crons → scripts → improve → log.

## 11. server-farm-manager (devops)
- POLICY: fires on fleet operations. Authority: CTO-grade autonomous ops; the ONLY gates = core-gateway restarts, deletes, credential rotation, clinical writes, money. Boundaries: the never-list (clinical data, firewalls without review, unapproved rotates).
- PROCEDURE: discover → fix → verify → report (the A-to-Z); the evidence discipline (probe outputs); the rituals (remote-gateway chain, container ops, creds hygiene, Hostinger lanes).

## 12. system-reliability-automation-ops (devops)
- POLICY: fires on stack audit/heal. Authority: the 12 validation capabilities; the self-healing MAY/NEVER lists. Boundaries: the A-to-Z delivery rule, the operational-only-after-e2e.
- PROCEDURE: discovery → health → e2e → API/MCP/DB/event/security/clinical-safety/backup validation → self-heal (MAY-list) → incident management (SEV-1..4) → the daily-ops report.

## 13. nura-cto-infrastructure-mandate (devops)
- POLICY: the standing CTO doctrine — every task, every session. Authority: the founder's mandate (2026-08-03). Boundaries: the access hierarchy (Atlas = org-lane only, Hermes = infrastructure), the minimum-necessary scope.
- PROCEDURE: the 4 responsibilities (infra monitoring · delegation · security · CI/CD) — the brief summaries with hard data; the exhaustive specs for developers.

## 14. decision-engine (software-development)
- POLICY: fires BEFORE every decision, build, skill, or task. Authority: the founder's first-principles doctrine. Boundaries: no ambiguity, no fabricated status.
- PROCEDURE: problem → first-principles → options → decision → route → build → verify → log.

## 15. hermes-agent (autonomous-ai-agents — the hub)
- POLICY: fires on ANY Hermes configuration/setup/troubleshooting. Authority: the official docs = the source of truth. Boundaries: the hard invariants (never break prompt caching, secrets in .env only, never hand-edit config.yaml).
- PROCEDURE: route via the reference table → load the matching reference → verify against the docs/live CLI → apply → evidence.

---

## The coverage doctrine
- The full library (250+ skills) follows the SAME P&P shape; the 15 above = the populated core (the safety + the governance + the ops spine). The remaining = the skeleton-registered in the skill index (each skill's own SKILL.md IS its procedure; the policy layer = the trigger + the boundaries embedded in each).
- The maintenance: the P&P manual updates with every new skill (the Skill Factory + the dojo watch) and every founder correction.
- The audit: the monthly Evolution Review verifies the manual against the live library (the drift-check).
