# DOXIMITY PUBLIC INSPECTION INDEX + NURA VALUE MAP (2026-08-05, founder-supplied)

**The complete public inspection: 62 public repos + the developer/API docs. The index = the competitive-intel + the reusable-resource map. The NURA strategy: study + adapt the high-value patterns; never copy proprietary code.**

## The high-value picks for NURA
1. **TARKA** (doximity/tarka) — ⭐ THE operational-AI-agent: the playbook system, the investigation pipeline, the diagnostic modules, memory, multi-provider LLM, Slack + GitHub integrations, the full docs-hub, the example reports (pod-crashloop!). = the NURA incident-commander's reference architecture — the adapt (playbooks + the investigation pipeline + the memory) into our incident-management skill!
2. **omniauth-doximity-oauth2** — the "Sign in with Doximity" OAuth2/OIDC strategy — the scopes: openid · profile:read:basic · profile:read:email · PKCE · refresh-tokens · JWT/OIDC subjects. = the physician-identity verification lane — the NURA physician-network's auth pattern (and a potential integration: our apps accept "Sign in with Doximity" = the physician-network attraction!).
3. **CallWithDoxDialer (iOS) + android-dialer-call-lib** — the physician dialer SDKs (dialPhoneNumber · startVoiceCall · startVideoCall · isInstalled). = the NURA app's call-lane reference.
4. **oauth2c** — the generic OAuth2 client (the tool for the token flows).
5. **phaxio-ruby** — the fax client (the Documo-parallel — the fax-lane reference).
6. **neo4j-http** — the Neo4j HTTP client (the graph-lane — the memory-graph reference).
7. **simplekiq** — the Sidekiq wrapper (the queue-ops).
8. **defect-dojo-client-go + terraform-provider-defectdojo + django-DefectDojo** — the security-vulnerability management (the pentest/vuln-tracker lane).
9. **hackerone-client** — the bug-bounty integration (the security-program lane).
10. **sumologic-sdk-ruby + sumologic-terraform-provider** — the logging/SIEM lane (the observability reference).

## The inspection links (the permanent index)
- Public repos: github.com/orgs/doximity/repositories · the code search patterns: org:doximity + (openapi | swagger | graphql | FHIR | HL7 | LLM | langchain | webhook | oauth | kubernetes | prometheus | neo4j | elasticsearch | sidekiq | CVE)
- The developer pages: doximity.com/developers/api_signup · /documentation · the OAuth issuer: auth.doximity.com · the Dialer: /clinicians/download/dialer
- The full 62-repo list: see the founder's index (the archived gems: pundit, rack-mini-profiler, uniqush-push, video.js...)

## The NURA thesis
- The Doximity moat = the physician network + the identity (the "Sign in with Doximity" = the provider-verification!) + the dialer (the physician-calling!). The NURA parallels: the physician-network data (the NPI/NPPES lane!), the identity (the Keycloak + the OAuth2-patterns!), the communications (the Twilio/voice!), the content (the corpora + the DocsGPT!).
- The Tarka = the direct architectural reference for our operational agent (the playbooks + the investigation + the memory — the incident-commander upgrade!).
