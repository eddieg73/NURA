# Hermes Daily Operations Report

**Date:** 2026-08-27 (02:05 UTC) · **Generated by:** Hermes / NURA OS · **Inventory:** `~/nura-ops/inventory.json` (engine `nura-inventory-health.py` v2026-08-27)

---

## Overall Status: **DEGRADED**

| Layer | Status | Detail |
|---|---|---|
| Container fleet | 🟢 Healthy | clinic **40/40** up · edge **8/8** up · lab **37/40** up |
| Critical endpoints | 🟢 Healthy | **9/9 open** (mirth · qdrant · openemr · mattermost · redis · npm · orthanc · hermes-gateway · api.nuratech.ai) |
| Scheduled automation | 🟠 Degraded | **58** ok · **27** error · **2** never-run (of 87 enabled) |
| Off-site backup | 🔴 Disabled | restic→Cloudflare R2 nightly **SKIPPED** (creds absent) |
| Clinical data plane | 🟢 Healthy | no failed clinical interfaces |

**Classification:** Infrastructure is healthy; the **automation/agent lane is impaired** and the **off-site backup is non-functional**. No patient-safety, breach, or major-outage event.

---

## Critical Incidents

- **SEV-3 — Dokploy management dashboard crash-looping (lab node 72.60.163.140).** `dokploy/dokploy:latest` swarm service is **0/1 replicas**. Migration completes (42P06 notice = benign), then `dist/server.mjs` exits **1** ~90s later; swarm reports `dockerexec: unhealthy container`. Container health `FailingStreak=8`. **No managed-app impact** — medplum, langfuse, LibreChat, akaunting, OHIF, nuratechwebsite all remain Up (independent of Dokploy). Non-clinical; dev/ops console only.
- **Instrumentation defect (RESOLVED) — false `UNHEALTHY: mirth,qdrant`.** The inventory engine's `CRITICAL` list probed stale host ports (mirth→8444, qdrant→32769, openemr→32776, mattermost/redis shifted) that no longer match the container-published ports. This falsely flagged two critical services as down and reported endpoints `7/9`. **Corrected** ports to container-published values and **re-verified 9/9 open** — mirth and qdrant were never down. No false alerts were acted on.

---

## Failed Integrations

**None.** All clinical/data integration surfaces verified open (container-published ports):
- `mirth` HL7 (admin 8445 · api 8086 · hl7 6663) — open
- `openemr` (32777) — open · `orthanc`/DICOM (8042) — open
- `medplum` FHIR (lab, Up 10 days) — healthy
- `api.nuratech.ai` (80) · `hermes-gateway` (8642) — open

---

## Backup Status: **DEGRADED**

- 🟢 **Paperclip runtime local SQL.gz:** healthy — latest `paperclip-20260827-011222.sql.gz` (01:12 UTC today); hourly cadence confirmed.
- 🔴 **Off-site restic→Cloudflare R2 nightly (`nura-backup.sh`):** **SKIPPED** — `RESTIC_PASSWORD` and `R2_API_TOKEN` **absent** from `/opt/data/profiles/nura/.env`. No off-site encrypted snapshot of Hermes config/skills/memories is being produced.
- 🔴 **AES-256 encrypted DB dumps (`backup_db.sh`):** no artifacts — `/var/backups/nuratech_db` does not exist.
- **Net:** local Paperclip DB recovery is safe; **there is no verified off-site restore path** for Hermes/HIPAA-adjacent state today.

---

## Security Status: **MONITORED — no incident**

- TLS: clinic `npm-8` fullchain valid to **Nov 1 2026** (nginx-proxy-manager). No expired/expiring cert observed.
- No unauthorized-access, login-anomaly, or exposed-service signal in this sweep.
- ⚠️ **DR/off-site gap** (see Backup Status): restic credentials not provisioned — a resilience, not a preventability, control.

---

## Clinical Safety Status: **HEALTHY**

- No failed clinical interfaces (mirth HL7 · openemr · orthanc · medplum all reachable).
- No delayed critical-result or unreviewed-AI-output escalation detected in this sweep.
- Clinical data plane (OpenEMR = source of truth path, mirth→HL7, orthanc→DICOM, medplum→FHIR) intact; no silent clinical-info drop observed.

---

## Resource Utilization

| Node | Host | Mem (used/total) | Disk |
|---|---|---|---|
| clinic | 72.61.71.211 | 12.7 / 32.0 GB (~40%) | 64% |
| lab | 72.60.163.140 | 11.6 / 32.0 GB (~36%) | 66% |
| edge | 195.35.32.113 | 2.1 / 3.9 GB (~54%) | 29% |

No swap alarm (swap-monitor cron last_status **ok**, every 30m). No node under disk or memory pressure requiring action.

---

## Scheduled Jobs: **26 of 87 failing**

**OpenRouter 402 — Insufficient credits (10 jobs):** org sweep · autonomy audit · mail triage · morning briefing · x check-in · hurricane watch · self-reflection · self-improvement daily · agent intel · dev governance. *(Daily agent chain blocked on OpenRouter balance.)*

**llama3.1:8b — "does not support thinking" HTTP 400 (10 jobs):** medical blog · self-improvement · space audit · clinical literature · marine forecast · drift audit · weekly snapshot · self-model review · competitive watch · obsidian-weekly. *(Model fallback resolves to local llama3.1:8b which rejects the thinking param.)*

**Other (6 jobs):**
- `moltbook morning/midday/evening` — script `moltbook-human-checkin.py` not found (3).
- `obsidian-morning` — TERMINAL_CWD write-lock timeout after 660s (lock contention).
- `emos gap audit` — playwright not importable in profile venv.
- `moltbook mining` — model context window 32k too small.
- `sleep consolidation` — intentional `drift_skip:silent` guard (working as designed, not a failure).

**Healthy examples:** swap monitor (ok) · incident audit (ok, completed 586) · skill availability (ok) · weekly scrum (ok) · evolution review (ok).

---

## Recommended Actions (Top 3)

1. **Top up OpenRouter credits** — the 402 error is blocking **10 daily automation jobs** (incl. morning briefing, autonomy audit, mail triage, org sweep) that feed the founder's dashboards. Operator: add credits/set budget at `openrouter.ai/settings/credits`; then re-run the affected jobs to confirm recovery.
2. **Fix the llama3.1:8b "does not support thinking" 400 across 10 jobs** — jobs are hitting a model that rejects the reasoning flag. Align each job to a thinking-capable model (e.g. deepseek-v4-flash-vision-exp) or disable `thinking` on the llama fallback; verify one, then propagate to the set.
3. **Re-enable the off-site restic→R2 nightly backup + repair Dokploy** — (a) provision `R2_API_TOKEN` and `RESTIC_PASSWORD` into `/opt/data/profiles/nura/.env` so `nura-backup.sh` runs (script already armed; only creds missing); (b) investigate the Dokploy `500` crash loop on the lab node (server.mjs exit post-migration / healthcheck failure) and restore the management dashboard; (c) restore the `moltbook-human-checkin.py` script path to unblock 3 jobs.

---

*Evidence: inventory engine status line — `clinic:40/40up | lab:37/40up | edge:8/8up | endpoints: 9/9 open` (after port correction). Probe output verified via direct socket checks against container-published ports. Cron roster parsed from `profiles/nura/cron/jobs.json` (87 enabled).*
