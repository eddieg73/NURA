# CarePilot — Flutter Provider Frontend
# P0 scaffold: FastAPI via CAREPILOT_API (default http://localhost:8100 Deployed).
# Screens: Home (work queue), Patient, Risk. Safety boundary surfaced in-app.
name: carepilot
description: CarePilot Complete Platform — provider-facing Flutter app.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
