{
  "sensor_layers": "Three physical layers are implied by the spec but only two are actually provisioned. LAYER 1 (primary nav/safety): the LDROBOT LD19 2D DToF LiDAR — 0.02-12 m, 4500 Hz ranging, 10 Hz scan, 360 deg, single horizontal plane, 0.8 deg angular resolution, +/-45 mm, 30 Klux ambient, -10C..+40C, 47 g, 0.9 W, UART 230400 one-way, 10,000 h motor life, ~$135. LAYER 2 (passive optical): EO forward/down cameras (the spec's 'vision cameras (EO forward/down)'), thermal/IR, IR illuminator + spotlight, plus the compute-side depth stack — Depth-Anything-3 / ros2-depth-anything-v3-trt (camera->metric point cloud on ROS2/TensorRT), Pointcept, CSF, patchwork, depth_clustering, pyRANSAC-3D, LEO, G2VLM — all permissive (Apache-2.0/MIT/BSD). LAYER 3 (missing in the plan): a nadir-facing range/clearance sensor. The spec's other nav sensors (RTK GPS, mmWave radar, dual IMU/baro/mag, ADS-B, Remote ID) and scene sensors (thermal, mic array, strobe/PA) are separately stated and are NOT satisfied or replaced by the LiDAR/camera stack. Mass and power are the one clean pass: 47 g and 0.9 W for the LD19 against a 15 lb payload pod and an Orin-Nano-class 7-15 W budget is ~6% of the power envelope and <1% of payload mass, and the -10C..+40C / 30 Klux envelope holds for Florida surface ops but not for the spec's 300-400 ft hover envelope or direct midday sun.",
  "summary": "The spec's sensing language is written as if one LiDAR delivers a 3D obstacle field, 3D SLAM, scene classification and verifiable landing zones, but the specified device class (LD19-class 2D DToF, 12 m, one horizontal plane) can only deliver a single-plane obstacle ring inside 12 m. Of the spec's LiDAR-assigned duties — SLAM mapping, obstacle field, precision landing, LiDAR-verified landing zone, LiDAR + vision + thermal scene classification, LiDAR-verified descent to patient — the 2D plane alone satisfies none of them outright; it partially serves lateral clearance and short-range obstacle detection. The camera-plus-depth stack recovers most of the missing 3D geometry with zero new hardware (dense metric point cloud via Depth-Anything-3 TensorRT on the existing Orin Nano), but three things remain unaddressed by any hardware in the plan: (a) below-vehicle / under-airframe clearance and ground contact, which a horizontal plane physically cannot observe; (b) wire/hazard detection, which is outside the scan plane and finer than the 0.8 deg beam spacing at range; (c) the spec's own listed non-optical requirements (gas detection CO/LEL/H2S, radiation, wind/weather probe) which no LiDAR or camera can substitute for. Secondary but real: the 12 m / 10 Hz envelope yields 0.38 s reaction at the spec's 70 mph dash, the 30 Klux rating is ~3.3x under clear-day sun, and the 12 m range covers under 13% of the spec's own 300-400 ft scene-assess hover altitude. Cheapest credible remediation is one downward rangefinder per drone ($25-40), one spare LiDAR per dock ($135), a documented sensor-degradation ladder, and an LD19-accurate SITL sensor model — no architecture change.",
  "gaps": [
    {
      "requirement": "SENSOR SUITE: NAVIGATION: 'LiDAR (SLAM mapping, obstacle field, precision landing)' — SLAM mapping component.",
      "why_unmet": "The LD19 is 2D: 360 deg in ONE horizontal plane, 10 Hz, 450 points/rev. Scan-matching a single plane yields 2.5D localisation with no roll/pitch-plane structure and no vertically observable geometry (facades, canopy, slope), and no Z correction on its own. A 450-point-per-sweep planar scan is far too sparse for 3D mapping of a scene, which the spec also demands ('3D maps' in RATCHET-Drone-Fleet-Spec.md, Scout Drone).",
      "cheapest_fix": "Zero hardware: make the camera-depth stack the 3D mapping source and keep the LD19 as the planar safety layer. Run ros2-depth-anything-v3-trt (TensorRT) on the existing Orin Nano to emit a metric point cloud, then Pointcept (segmentation) + pyRANSAC-3D / CSF (ground) for map structure, fused with the spec's RTK GPS (already stated as 'constant GPS comm (RTK cm-precision)') for global pose. Optional, ~$135: a second LD19 rotated 90 deg (nadir) to give one crossing plane.",
      "severity": "HIGH"
    },
    {
      "requirement": "SENSOR SUITE: NAVIGATION: 'LiDAR (SLAM mapping, obstacle field, precision landing)' — obstacle field component, and Avionics: 'sense-and-avoid (camera + radar + LiDAR — Amazon-model)' at 'Performance: 15-25 mi range - 50-70 mph dash'.",
      "why_unmet": "Max range 12 m. At the spec's own dash speed that is 0.38 s of reaction at 70 mph (12 / 31.3 m/s) and 0.54 s at 50 mph (12 / 22.35 m/s), and the 10 Hz sweep adds 3.1 m of blind travel per scan at 70 mph (2.2 m at 50 mph). No 15-55 lb airframe closes a sense-and-avoid loop on 0.38 s with a 100 ms sensor period. The spec assigns transit-phase avoidance to 'camera + radar + LiDAR' jointly, but the LiDAR limb of that triple cannot carry transit speeds.",
      "cheapest_fix": "Re-scope by envelope, no purchase: designate the LD19 for terminal and low-speed phases only (usable to 12 m and <=5 m/s) and let the spec's already-stated mmWave radar ('rain/fog/dust that kills LiDAR') plus EO camera carry 50-70 mph transit avoidance. Write the envelope into the SITL mission profiles so 'Simulation-first: Gazebo/SITL validate every mission profile before flight' validates the real division of labour instead of an assumed 3D LiDAR.",
      "severity": "HIGH"
    },
    {
      "requirement": "ARRIVAL / SCENE ASSESS: 'LiDAR + vision + thermal classify the scene - caller/patient located? landing zone clear/obstructed/water? hazards (wires, traffic)?'",
      "why_unmet": "Two independent failures. (1) Hover altitude: the spec states 'hover at safe altitude (300-400 ft default -> descend)' = 91-122 m, but the LD19 returns nothing beyond 12 m — it covers 9.8-13.1% of the stated hover height. (2) Hazards 'wires': a conductor above or below the scan plane cannot be intersected by a horizontal plane at all, and even for an in-plane obstacle the 0.8 deg beam spacing is 16.8 cm at 12 m (2 * 12 * tan 0.4 deg), so 5-10 mm conductors and thin branches fall between beams. 'Water' is also not an LD19 job: specular grazing returns from water at altitude are unreliable.",
      "cheapest_fix": "Move scene classification off the LiDAR at altitude. Classify from EO + thermal + monocular depth at the 300-400 ft hold (Depth-Anything-3 relative height field; G2VLM / LEO for scene reasoning on the Orin Nano), and bring the LD19 into the loop only below ~12 m AGL during final descent. Detect water from EO texture plus low thermal contrast (near-zero delta-T), not from the LiDAR. Formally map 'wires' to camera + mmWave radar and keep the existing mode-select row 'Hazard (wires/traffic/storm) -> HOVER at safe altitude -> abort to RTL if escalating' as the wire rule, so the LiDAR is never the wire sensor. Documentation-only cost.",
      "severity": "CRITICAL"
    },
    {
      "requirement": "SAFE LANDING PROTOCOL: 'LiDAR-verified clear landing zone -> auto-land' and RECOVERY: 'RTL to truck roof dock (RTK cm landing)'.",
      "why_unmet": "A single horizontal plane at landing altitude gives an obstacle RING at the vehicle's current height, not a clearance measurement below it. It provides no terrain height map, no slope estimate and no altitude above ground; +/-45 mm ranging error plus 16.8 cm beam spacing at 12 m undersamples small obstructions (curb, rock, limb, cable) inside the landing disc. 'LiDAR-verified clear' as written cannot be evidenced by a nadir-blind plane, and cm-precision touchdown needs a downward range, which no sensor in the plan provides.",
      "cheapest_fix": "Add ONE downward-looking rangefinder per drone and feed it as a standard DISTANCE_SENSOR / MAVLink altitude source on the existing PX4/ArduPilot stack so the ArduPilot/PX4 landing logic and the existing 'LiDAR-verified' language both become true. Benewake TF-Luna ~$25 (0.2-8 m) is sufficient for the final 8 m of descent at the spec's auto-land speeds; TFmini-S ~$40 (0.1-12 m) if the clearance must match the LD19's own 12 m. Use the truck RTK base for dock-relative XY, as the spec already states.",
      "severity": "CRITICAL"
    },
    {
      "requirement": "PATIENT PROTOCOL: 'patient identified: descend toward patient (LiDAR-verified)'.",
      "why_unmet": "Descent toward a target directly beneath the airframe is outside a horizontal scan plane, and the under-vehicle region is occluded by the airframe and landing gear (mounting geometry INFERRED — the LD19's 360 deg horizontal disc does not extend to the blind volume immediately below the body). The LD19's 0.02 m minimum range constrains where it can even be mounted. Nothing in the plan measures patient-relative pose or vertical closure during the descent.",
      "cheapest_fix": "Same single downward rangefinder as the landing gap (TF-Luna ~$25) covers vertical closure, plus the spec's own nadir EO camera ('vision cameras (EO forward/down)') with monocular depth for patient-relative pose. Treat the LD19 as lateral clearance only in this state. One sensor purchase covers this gap and the landing gap — do not buy twice.",
      "severity": "HIGH"
    },
    {
      "requirement": "EDGE COMPUTE & COMMS ON EVERY DRONE: 'small Jetson (Orin-Nano class) per drone - onboard AI: vision (scene, target, AED-drop verification, landing zone), sense-and-avoid assist, payload status, telemetry preprocessing - LOCAL-AI doctrine same as trucks (offline-capable, deterministic safety underneath)' at '7-15W'.",
      "why_unmet": "The LiDAR's inability to provide 3D geometry offloads SLAM, scene classification and landing-zone reasoning onto the dense-depth network, which is the single most expensive model in the stack (per-frame full-resolution encoder pass). Running a TensorRT depth model (ros2-depth-anything-v3-trt), plus EO/thermal detection, plus point-cloud segmentation (Pointcept), plus sense-and-avoid assist, concurrently inside a 7-15 W envelope on Orin-Nano-class silicon is not achievable at flight rate without model selection and duty-cycling (exact achievable FPS on Orin Nano at 15 W is INFERRED, not measured here — treat any single number as needing bench confirmation).",
      "cheapest_fix": "Zero hardware, configuration only: run the depth model as a small variant (ViT-S class) at reduced input resolution (322-518 px) in FP16/INT8 via TensorRT, duty-cycled to ARRIVAL/descend states at 5-10 Hz, while the LD19 obstacle ring runs always-on at 10 Hz as the cheap safety layer. Keep the deterministic EMD/clinical path on the MCU/companion, never on the depth model, per the spec's own 'deterministic safety underneath' and 'ZERO cloud dependency for clinical function'. Validate the resulting frame budget in the existing SITL/Gazebo profiles before flight.",
      "severity": "HIGH"
    },
    {
      "requirement": "Simulation-first: 'Gazebo/SITL validate every mission profile before flight' and autonomy doctrine 'sim-validated mission profiles first (SITL/Gazebo - never fly unvalidated)'.",
      "why_unmet": "A 2D 450-sample / 10 Hz single-plane scan plus a monocular-depth camera presents a completely different avoidance geometry from the 3D LiDAR plugin a default SITL world assumes. Any mission profile already 'validated' in simulation would therefore validate the wrong sensor model — the geometry that matters (planar blind spots, beam spacing at range, absence of below-vehicle data) is exactly what a generic 3D LiDAR plugin papers over.",
      "cheapest_fix": "Config plus documentation, no hardware: author a Gazebo ray sensor matching the LD19 exactly (12 m max, 450 samples, 0.8 deg angular resolution, 10 Hz, single plane, range/ambient masks for the 30 Klux limit) alongside a camera-depth node wired to the same TensorRT model as the flight stack, and re-run the existing mission profiles against it. Gate flight release on those re-runs.",
      "severity": "MEDIUM"
    },
    {
      "requirement": "SENSOR SUITE: NAVIGATION: 'radar (mmWave - rain/fog/dust that kills LiDAR)' combined with '30 Klux ambient' class optics: a stated all-weather/all-lighting sensing duty.",
      "why_unmet": "The spec names rain/fog/dust as the LiDAR killer but does not state the daytime counterpart. A clear-day sunlit scene is roughly 100,000 lux, about 3.3x the LD19's 30 Klux ambient rating, so the LiDAR's ranging degrades in exactly the high-ambient conditions when the EO/depth lanes are also degraded by specular glare and blown highlights. When the radar limb is itself rain-attenuated and optics are glare-limited, the plan states no complementary sensing for the combined degradation case.",
      "cheapest_fix": "Documentation plus a small consumable: publish a sensor-degradation ladder in the spec (RTK + dual IMU dead-reckoning -> mmWave radar proximity -> HOVER -> RTL) with explicit ambient/visibility thresholds tied to the 30 Klux rating, and add a hydrophobic/anti-glare lens cover (low single-digit dollars per drone) to the EO/thermal apertures. No new sensing channel required; the spec's own failsafe hierarchy and mode-select table already terminate correctly — they just are not tied to sensor-envelope numbers.",
      "severity": "MEDIUM"
    },
    {
      "requirement": "Fleet ops / Digital Twin: 'maintenance prognostics', 'predicted failures', 'sensor calibration' (EMS spec Fleet ops + RATCHET-Drone-Fleet-Spec.md Digital Twin Fleet), in the context of 'LiDAR-verified' landing and descent.",
      "why_unmet": "The LD19 is a spinning-mechanism device with a finite 10,000 h motor life and a one-way UART (230400, no command channel), so it cannot report its own health: no spin-rate sanity, no temperature, no fault status to ingest. That makes it an unmonitored single point of failure for two safety-critical spec functions ('LiDAR-verified clear landing zone', 'LiDAR-verified' descent), and the plan has no declared failover when the scan stops or degrades.",
      "cheapest_fix": "Zero hardware: run a companion-side LiDAR driver that validates each sweep (point count versus the expected 450, angle monotonicity, max/min range sanity) and publishes health plus a MAVLink OBSTACLE_DISTANCE/DISTANCE_SENSOR stream, with the validation result written to the spec's own 'flight data recorder (black box - tamper-evident)'. Add a hard rule: any failed or stale sweep drops the drone to the existing 'Hazard -> HOVER -> RTL' row. Track cumulative spin hours in the digital-twin lane and schedule swap at ~8,000 h (80% of 10,000 h); carry one $135 spare LD19 per dock.",
      "severity": "HIGH"
    },
    {
      "requirement": "SENSOR SUITE: ENVIRONMENT: 'optional pods: gas detection (CO/LEL/H2S) + radiation probe (HazMat/MCI scenes - disaster-response tie)' and 'onboard weather probe (wind/temp for landing + cold-chain ambient)'.",
      "why_unmet": "Unaddressed entirely, and unaddressable by the sensing under audit: CO, LEL, H2S and radiation are non-optical quantities, so neither the 2D LiDAR nor the camera-plus-depth stack contributes anything; likewise no optical sensor measures wind. The spec lists these as requirements (weather probe is not marked optional) and the plan states no implementation, no pod-bus electrical/interface allocation and no cost.",
      "cheapest_fix": "Use the already-specified pod bus ('BLE (payload pods, ground links)') rather than a new architecture: one low-cost CO/LEL/H2S module on the pod connector for HazMat scenes (INFERRED module cost range, single-digit hundreds of dollars; verify against an off-the-shelf 3-gas module), keep the radiation probe as a genuinely optional pod, and derive wind speed with zero hardware from the existing IMU plus ESC thrust-versus-attitude estimate. Add a $10-20 temperature/humidity sensor to the pod bus for the spec's cold-chain ambient and landing-weather duties.",
      "severity": "MEDIUM"
    },
    {
      "requirement": "OVERWATCH MISSION MODE and TACTICAL MEDICAL ROLE: 'thermal (victim/subject/vehicle detection)', 'casualty location via thermal (who's down, where, moving?)', 'perimeter alert (movement outside scene = warning to crew)', 'route/cover analysis'.",
      "why_unmet": "The sensor named here (thermal/IR) is stated and available, so the gap is not the sensor — it is that the audit stack supplies no 3D geometry to convert a thermal detection into a located casualty or a covered approach: a planar 2D scan gives no elevation model for terrain, cover or approach analysis, and the LD19's 12 m reach is far inside the overwatch standoff. Nothing in the plan produces the elevation data that 'route/cover analysis' and 'landing coordinates for CASEVAC' imply (RATCHET spec claims '3D maps' for the Scout Drone).",
      "cheapest_fix": "Build cover/approach geometry from the camera-depth point cloud rather than from the LiDAR: ros2-depth-anything-v3-trt -> Pointcept/CSF ground separation -> a coarse elevation and obstacle map published to the truck console and the TAK pin, refreshed only while the drone is in a stable hover. Zero hardware. Where standoff exceeds useful monocular-depth range, mark the map as relative (not survey-grade) rather than claiming 3D mapping accuracy.",
      "severity": "MEDIUM"
    },
    {
      "requirement": "PATIENT PROTOCOL 'deliver pod (precision drop or winch)' and PODS 'precision drop + winch (lower pods where landing is impossible: brush, water, windows, roofs - no free-drop of fragile/medical pods)'.",
      "why_unmet": "Winch/drop below the airframe is the same nadir blind volume the 2D LiDAR cannot see, and nothing in the sensor list measures tether payout or ground contact, so 'no free-drop' cannot be enforced by sensing as the plan stands. Monocular depth alone cannot resolve the centimetre-scale clearance needed at the point of pod release, and it carries monocular scale ambiguity absent an absolute reference.",
      "cheapest_fix": "The same single downward rangefinder already needed for landing and descent (TF-Luna ~$25) supplies ground-clearance for the winch, combined with winch encoder/tension feedback on the pod bus (mechanical, low-cost). Use RTK/IMU for absolute scale on the depth lane, per the landing fix — one purchase serves three spec functions.",
      "severity": "MEDIUM"
    },
    {
      "requirement": "RATCHET-Drone-Fleet-Spec.md Scout Drone: 'camera-based photoplethysmography pulse estimation (where conditions permit)', 'detect movement/breathing', 'count casualties'; plus EMS spec 'vitals/visual monitoring streamed (NEWS2 context)'.",
      "why_unmet": "INFRED: measurement conditions are not specified and rPPG is incompatible with the platform as described — a 10 Hz-planar-LiDAR + moving-airframe camera stack has no stated frame rate, gimbal or stabilisation requirement, and the EMS spec's cited motion envelope is '50-70 mph dash' with loiter '10-15 min at altitude'. The parenthetical 'where conditions permit' means this is not currently a satisfied requirement and the plan does not state what would satisfy it.",
      "cheapest_fix": "Cheapest credible fix is scope discipline, then sensing: reroute the claim to 'visual casualty detection plus movement/breathing motion analysis' from the existing EO/thermal (LEO / G2VLM on the Orin Nano), computed only in a stable hover and processed on the truck console, and delete or explicitly gate the rPPG claim. If rPPG is retained, add a hover-stabilised gimbal and a stated minimum frame rate to the spec as hard requirements — that is the only path to making the existing sentence true.",
      "severity": "LOW"
    }
  ]
}