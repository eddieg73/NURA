```json
{
  "compatible": true,
  "pg_versions": "MAJOR-VERSION COMPATIBLE = YES. Snapshot data dir /opt/data/paperclip-runtime.bak-20260803/instances/default/db/PG_VERSION = 18; live data dir /opt/data/paperclip-runtime/instances/default/db/PG_VERSION = 18; live server reports PostgreSQL 18.1 (embedded binary /opt/data/profiles/nura/home/.npm/_npx/11538a0705b03038/node_modules/@embedded-postgres/linux-x64/native/bin/postgres, running as 'postgres -D /opt/data/paperclip-runtime/instances/default/db -p 54329'). Same major 18 -> no pg_upgrade, and a physical datadir swap would also be major-compatible. Schema/migration state also matches exactly, so there is no schema skew either: live = 157 tables + drizzle.__drizzle_migrations count 182 / max id 182; dump = 157 CREATE TABLE + setval(...id_seq, 182, true), 1440 statement chunks / 35 COPY blocks / 12509 rows. Restore mechanism caveat (not a version issue): this host has NO psql/pg_dump/pg_restore binary anywhere on the filesystem, and Paperclip 2026.722.0 ships no restore CLI (only db:backup) - see restore_steps/risks.",
  "requires_server_stop": false,
  "restore_steps": [
    "0) DO NOT STOP THE CONTROL PLANE FIRST. The embedded PostgreSQL (:54329) is a child of the paperclipai node process (parent chain: npm exec paperclipai@2026.722.0 onboard -> sh -> node -> postgres); stopping it kills PG and the restore has nothing to connect to. Restore while it runs, then restart it (step 8).",
    "1) Preflight - confirm the control plane and its DB are up: python3 /opt/data/scripts/paperclip_server_manager.py status  (verified right now: healthy=true db_healthy=true pid=592279 live_pids=[1713457,1713474,1713475] url=http://127.0.0.1:3100).",
    "2) Dry-run the copy-aware driver against the snapshot - parses the whole dump, sends NO SQL (verified: DRY-RUN statements=1405 copy_blocks=35 copy_rows=12509 ms=982): node /opt/data/scripts/paperclip_restore_copyaware.mjs \"postgres://paperclip:paperclip@127.0.0.1:54329/paperclip\" /opt/data/paperclip-runtime.bak-20260803/instances/default/data/backups/paperclip-20260803-213533.sql.gz --dry-run",
    "3) Create the rollback artifact from the CURRENT (empty) live DB - read-only w.r.t. the DB, ~36.7 KB: mkdir -p /opt/data/paperclip-rollback-<ts> ; npx --yes paperclipai@2026.722.0 db:backup --data-dir /opt/data/paperclip-runtime --dir /opt/data/paperclip-rollback-<ts> --json  (verified equivalent invocation: /opt/data/profiles/nura/home/.npm/_npx/11538a0705b03038/node_modules/.bin/paperclipai db:backup --data-dir /opt/data/paperclip-runtime --dir <dir> --json -> rc=0, paperclip-20260912-180148.sql.gz 37567 bytes).",
    "4) Preserve the current secrets key so the key swap is reversible: cp -p /opt/data/paperclip-runtime/instances/default/secrets/master.key /opt/data/paperclip-runtime/instances/default/secrets/master.key.live-<ts> ; chmod 600 /opt/data/paperclip-runtime/instances/default/secrets/master.key.live-<ts>",
    "5) RUN THE RESTORE (the actual restore). One connection, inside the dump's own BEGIN...COMMIT, ~6 s, DROPs + re-creates all 157 tables. Run at, say, :20 past the hour - automatic Paperclip backups fire at HH:10 - and while no agent work is queued: node /opt/data/scripts/paperclip_restore_copyaware.mjs \"postgres://paperclip:paperclip@127.0.0.1:54329/paperclip\" /opt/data/paperclip-runtime.bak-20260803/instances/default/data/backups/paperclip-20260803-213533.sql.gz   Expected output: RESTORE-OK statements=1405 copy_blocks=35 copy_rows=12509 ms=<~6000>",
    "6) REQUIRED KEY SWAP - the restored rows are ciphertext encrypted with the snapshot's key (live md5 2a885778a7632f73377997c758a01b15 != snapshot md5 3896b4415ba613eb1b873921584bbd82). The live instance owns zero secrets, so nothing is lost: cp -p /opt/data/paperclip-runtime.bak-20260803/instances/default/secrets/master.key /opt/data/paperclip-runtime/instances/default/secrets/master.key ; chmod 600 /opt/data/paperclip-runtime/instances/default/secrets/master.key",
    "7) Verify restored contents (read-only): node /opt/data/scripts/paperclip_db_verify.mjs   Expected: server_version 18.1, tables 157, migrations 182/182, companies 999ff375-6128-41cf-b6c8-06b98673a29b 'Nuratech.ai' (NUR, counter 147) and 425ddbd7-721a-4e4a-80d9-bb339762c6df 'NURA Imaging SaaS Division', agents 72, issues 148, user local-board, env 'Local' id c58ebac0-fe20-4b0c-a11d-3d0338d97864, secrets 46. (Every one of these figures was produced in a full rehearsal restore - see risks #3.)",
    "8) Restart the control plane so it drops stale connections/prepared statements and re-reads the new secrets key. The manager's stop() only acts on hermes-paperclip.pid, which can be stale or missing, so pin the real group leader first (the ^ anchor keeps pgrep from matching your own shell): CP=$(pgrep -f '^npm exec paperclipai@2026.722.0 onboard' | head -1) ; echo \"$CP\" > /opt/data/paperclip-runtime/hermes-paperclip.pid ; chmod 600 /opt/data/paperclip-runtime/hermes-paperclip.pid ; python3 /opt/data/scripts/paperclip_server_manager.py restart ; python3 /opt/data/paperclip_server_manager.py status   (expected healthy=true db_healthy=true). NOT rehearsed - it would interrupt the live plane; the kill/start logic itself is the existing, in-use manager script.",
    "9) Fix config drift after the restore: edit /opt/data/paperclip-runtime/mcp.env -> PAPERCLIP_COMPANY_ID=999ff375-6128-41cf-b6c8-06b98673a29b (the current value 58ddc931-7dbb-44c3-ab34-2652571121fc exists in neither DB and 404s), and set a fresh PAPERCLIP_API_KEY (currently empty; the dump's board_api_keys table is empty, so the old pcp_board_bc61... token from the .bak mcp.env will not authenticate - mint a new one with the paperclipai token / board-claim flow).",
    "10) Alternative mechanism (NOT rehearsed): install a psql client (Debian 13 host, apt-get present, network OK, but you are user hermes, so root is needed), e.g. apt-get install -y postgresql-client, then export PAPERCLIP_PSQL_PATH=/usr/bin/psql and call Paperclip's own runDatabaseRestore, which runs 'psql --dbname=<connstr> --set=ON_ERROR_STOP=1 --quiet --no-psqlrc' with the gunzipped dump on stdin: gunzip -c /opt/data/paperclip-runtime.bak-20260803/instances/default/data/backups/paperclip-20260803-213533.sql.gz | psql \"postgres://paperclip:paperclip@127.0.0.1:54329/paperclip\" -v ON_ERROR_STOP=1 --quiet --no-psqlrc   (docker is also present: docker run --rm -i postgres:18 psql ... would work the same way). Prefer step 5 - it is the verified path.",
    "11) Connection facts used above: database 'paperclip', role 'paperclip', password 'paperclip', host 127.0.0.1:54329, pg_hba = 'password' for local and 127.0.0.1. Verified by a read-only SELECT session (SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY) against the live server. The dump issues SET LOCAL session_replication_role = replica, so the role must be superuser - the 'paperclip' role is the cluster bootstrap superuser here."
  ],
  "risks": [
    "1) TOOLING GAP - THE STOCK RESTORE PATH DOES NOT WORK HERE (verified by rehearsal, not assumed). No psql/pg_dump/pg_restore exists on this filesystem, and Paperclip's library restore runDatabaseRestore tries psql first, then a postgres.js fallback. Against this dump it fails twice: 'psql error: spawn psql ENOENT', then 'Failed to restore ... ERROR: syntax error at or near \"1\" [statement: COPY \"drizzle\".\"__drizzle_migrations\" (\"id\", \"hash\", \"created_at\") FROM stdin;]' - postgres.js cannot execute an inline COPY...FROM stdin payload, and the dump has 35 such blocks. The installed driver /opt/data/scripts/paperclip_restore_copyaware.mjs fixes this by feeding each COPY block through postgres.js's streaming .writable() COPY API (payload never re-encoded); it was rehearsed successfully end-to-end. Do not attempt the restore from the CLI/library as-is.",
    "2) SECRETS KEY MISMATCH - biggest silent-failure risk. The live secrets/master.key and the snapshot's are different files (md5 2a885778a7632f73377997c758a01b15 vs 3896b4415ba613eb1b873921584bbd82). All 46 restored public.company_secrets / company_secret_versions rows are local_encrypted_v1 ciphertext (46 rows carry the fingerprint 5d69e10d31...); they decrypt ONLY with the snapshot key. If step 6 is skipped, every agent's hermes_gateway apiKey secret_ref fails to resolve and the restored agent fleet (44 agents reference secretIds) cannot authenticate. The live instance has 0 company_secrets, so swapping the key destroys nothing.",
    "3) DESTRUCTIVE BUT ATOMIC; LITTLE TO LOSE. The dump DROP TABLE IF EXISTS ... CASCADE for all 157 tables inside a single BEGIN...COMMIT, so it either fully applies or fully rolls back - verified: a failed rehearsal attempt left the target at 0 tables with no partial state. A successful COMMIT is irreversible without the step-3 backup. What is actually lost is negligible and was enumerated: the live DB holds 186 rows across 5 tables (182 drizzle migration rows, user 'local-board', environment 'Local', instance_settings, instance_user_roles) and 0 rows in companies/agents/issues/activity_log/company_secrets. The snapshot recreates all of those with the same identity (user id 'local-board', role instance_admin), so no live data is discarded. Also lost: whatever is not in the snapshot at all - attachment/asset blobs (data/storage contains 0 files in BOTH trees), run log artifacts (heartbeat_runs/workspace_operations rows reference /opt/data/paperclip-runtime/instances/default/workspaces/<uuid>/, which does not exist), and any work done between 2026-08-03 21:35 and now (the live DB never contained any).",
    "4) CONCURRENCY DURING THE RESTORE. The restore runs against a live cluster with the app connected (9 idle postgres sessions observed). Its ACCESS EXCLUSIVE locks make concurrent app queries block for the duration (~5.5 s in the full rehearsal: 12509 rows, result 55 MB). Avoid the top of the hour - the app fires an automatic backup at HH:10 (log evidence: 'Automatic database backup starting ... 17:10:31 ... pruned 1 old backup(s)') - and avoid running it while agents are mid-run. Do not stop the control plane to 'quiesce' it: that takes PG down with it (step 0).",
    "5) POST-RESTORE STALENESS / RESTART REQUIRED. The running app caches companies, environments and instance settings and holds idle pooled connections; it must be restarted (step 8) to pick up the restored data and the swapped secrets key. Because the schema is structurally identical (same 157 tables, same 182 migrations), prepared statements do not break - the restart is about cache/in-memory state, not schema.",
    "6) CONFIG DRIFT AFTER RESTORE. /opt/data/paperclip-runtime/mcp.env currently points at company 58ddc931-7dbb-44c3-ab34-2652571121fc, which exists in neither DB (the live log shows 'GET /58ddc931-... 404' and empty agents/issues responses) - the MCP lane will keep 404ing until it is repointed at 999ff375-6128-41cf-b6c8-06b98673a29b. PAPERCLIP_API_KEY is empty and board_api_keys/agent_api_keys are empty in the dump, so board/agent API credentials must be re-minted; the old pcp_board_bc61... token in the .bak mcp.env will not authenticate.",
    "7) SENSITIVE PAYLOAD. The snapshot dump contains plaintext credentials - for example agents.adapter_config stores at least one adapter apiKey in the clear alongside the secret_refs - plus encrypted secret material and business/clinical content (NUR-/NURA- issue titles, research and hiring directives, 149 document bodies). Treat the 2.3 MB .gz (~40 MB raw) as confidential; do not copy it off-host or into chat/logs. After restore that content is readable through the board UI for anyone with instance access.",
    "8) ENVIRONMENT ID CHANGES. The live environment is 99489c73-bf04-4751-9afb-1733080a5487 ('Local'); the snapshot's is c58ebac0-fe20-4b0c-a11d-3d0338d97864. After restore, instance_settings.default_environment_id and every agent/issue reference point at the snapshot's environment (internally consistent), but any external script, mcp.env entry or document that hard-codes the current environment id becomes stale.",
    "9) DO NOT USE THE PHYSICAL DATADIR ROUTE. The snapshot tree contains db_corrupted_20260731, evidence that cluster was damaged on 2026-07-31 (which is why dumps exist at all), and its postmaster.opts references a now-deleted npm prefix (/opt/data/.npm/_npx/...). The SQL dump from data/backups is the sanctioned artifact; a datadir swap would import a previously-corrupted cluster.",
    "10) EXTENSION/SUPERUSER DEPENDENCIES. The dump does CREATE EXTENSION IF NOT EXISTS fuzzystrmatch / pg_trgm (both present in this embedded build - verified in rehearsal, extensions list = fuzzystrmatch, pg_trgm, plpgsql) and SET LOCAL session_replication_role = replica, which needs superuser. On a locked-down role or a Postgres without contrib, the restore aborts at that point (and, because it is atomic, rolls back cleanly).",
    "11) FAILURE MODE IS SAFE. Because the whole file is one transaction, an interrupted or failed restore leaves the live DB exactly as it was (verified: the failed attempt produced 0 tables in the target and the live DB still reports 157 tables / 182 migrations / 0 companies afterwards). Re-run after fixing the cause; no partial-state cleanup is needed."
  ],
  "rollback": "Two moves, both rehearsed. (1) DB: the pre-restore state is captured by step 3's paperclipai db:backup artifact (also duplicated by the app's hourly backups in /opt/data/paperclip-runtime/instances/default/data/backups/ - newest paperclip-20260912-171031.sql.gz, 36.7 KB, 186 rows). To undo, run the SAME driver against that file: node /opt/data/scripts/paperclip_restore_copyaware.mjs \"postgres://paperclip:paperclip@127.0.0.1:54329/paperclip\" /opt/data/paperclip-rollback-<ts>/paperclip-<ts>.sql.gz  -> RESULT VERIFIED IN A THROWAWAY CLUSTER ON :54398: 'RESTORE-OK statements=1405 copy_blocks=5 copy_rows=186', and the resulting database was exactly the live state (157 tables, migrations 182/182, 0 companies, 0 agents, 0 issues, user 'local-board' / local@paperclip.local, environment 99489c73-bf04-4751-9afb-1733080a5487 'Local', instance_settings 1, instance_user_roles 1 = instance_admin, 0 secrets). (2) Secrets key: cp -p /opt/data/paperclip-runtime/instances/default/secrets/master.key.live-<ts> /opt/data/paperclip-runtime/instances/default/secrets/master.key && chmod 600 <same path>, then restart the control plane (step 8) so the old key is re-read. If a restore dies mid-run, do nothing - the transaction rolls back (verified) and the live DB is untouched. Nuclear option if the PG data directory itself is ever damaged: python3 /opt/data/scripts/paperclip_server_manager.py stop ; mv /opt/data/paperclip-runtime/instances/default/db /opt/data/paperclip-runtime/instances/default/db.broken-<ts> ; npx --yes paperclipai@2026.722.0 onboard --yes --bind loopback --data-dir /opt/data/paperclip-runtime (re-creates an empty, fully migrated 182-migration cluster) ; then restore the step-3 backup from (1) to get back to the pre-restore empty state."
}
```